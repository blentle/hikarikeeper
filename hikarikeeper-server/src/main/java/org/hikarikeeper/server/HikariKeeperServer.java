package org.hikarikeeper.server;

import org.hikarikeeper.core.HikariException;

/**
 * @desc a HikariKeeperServer represents a single server instance
 * @responsibilities resolve command line or bootstrap parameters and init server instance and start server
 */
public class HikariKeeperServer {

    /**
     * args from parameters
     * @param args
     */
    public HikariKeeperServer(String[] args) {

    }

    public void start() throws HikariException {
        //todo:sync
    }
}
