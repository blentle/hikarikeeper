package org.hikarikeeper.client.impl;

import org.hikarikeeper.client.HikariClient;
import org.hikarikeeper.client.HikariServerConfig;

public class DefaultHikariClient implements HikariClient {

    public DefaultHikariClient(HikariServerConfig config) {

    }
}
